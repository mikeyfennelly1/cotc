info:
  name: 0200 - Get sources by type
  type: http
  seq: 3

http:
  method: GET
  url: "{{web-app}}/api/sysinfo/sources?type=device"
  params:
    - name: type
      value: device
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
