info:
  name: 0100 - Device Types
  type: http
  seq: 1

http:
  method: GET
  url: "{{web-app}}/api/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
