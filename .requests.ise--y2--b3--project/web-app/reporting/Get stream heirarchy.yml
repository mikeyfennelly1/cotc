info:
  name: Get stream heirarchy
  type: http
  seq: 1

http:
  method: GET
  url: "{{web-app}}/api/reporting/streams/hierarchy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
