info:
  name: Subscribe to a stream
  type: http
  seq: 3

http:
  method: GET
  url: "{{web-app}}/api/reporting/streams"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
