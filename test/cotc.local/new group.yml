info:
  name: new group
  type: http
  seq: 1

http:
  method: POST
  url: "{{subscriber}}/api/group?name=test"
  params:
    - name: name
      value: test
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
