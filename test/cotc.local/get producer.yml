info:
  name: get producer
  type: http
  seq: 4

http:
  method: GET
  url: "{{subscriber}}/api/producer?name=clitest"
  params:
    - name: name
      value: clitest
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
