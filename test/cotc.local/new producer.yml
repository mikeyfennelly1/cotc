info:
  name: new producer
  type: http
  seq: 2

http:
  method: POST
  url: "{{subscriber}}/api/producer"
  body:
    type: json
    data: |-
      {
        "name": "test-producer",
        "group": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
