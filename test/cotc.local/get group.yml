info:
  name: get group
  type: http
  seq: 3

http:
  method: GET
  url: "{{subscriber}}/api/group?name=test"
  params:
    - name: name
      value: test
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
