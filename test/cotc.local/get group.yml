info:
  name: get group
  type: http
  seq: 3

http:
  method: GET
  url: "{{subscriber}}/api/group"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
