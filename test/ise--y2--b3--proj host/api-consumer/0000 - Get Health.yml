info:
  name: 0000 - Get Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{web-app}}/api/reporting/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
