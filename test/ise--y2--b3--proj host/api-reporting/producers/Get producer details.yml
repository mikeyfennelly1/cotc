info:
  name: Get producer details
  type: http
  seq: 5

http:
  method: GET
  url: "{{web-app}}/api/reporting/producers?name=sysinfo-reader--1"
  params:
    - name: name
      value: sysinfo-reader--1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
