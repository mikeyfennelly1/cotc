info:
  name: Get a specific stream
  type: http
  seq: 4

http:
  method: GET
  url: "{{web-app}}/api/reporting/streams?name=test.sequence1child"
  params:
    - name: name
      value: test.sequence1child
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
