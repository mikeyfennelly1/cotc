info:
  name: 0000 - Get health
  type: http
  seq: 1

http:
  method: GET
  url: "{{nginx}}/api/reporting/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
