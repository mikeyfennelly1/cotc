info:
  name: 0300 - Get producers for stream
  type: http
  seq: 3

http:
  method: GET
  url: "{{nginx}}/api/reporting/streams/5/producers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
