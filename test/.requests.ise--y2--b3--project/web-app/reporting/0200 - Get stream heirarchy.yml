info:
  name: 0200 - Get stream heirarchy
  type: http
  seq: 2

http:
  method: GET
  url: "{{nginx}}/api/reporting/streams/hierarchy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
