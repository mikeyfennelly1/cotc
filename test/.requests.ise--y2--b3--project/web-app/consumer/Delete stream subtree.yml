info:
  name: Delete stream subtree
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{nginx}}/api/consumer/streams?name=test"
  params:
    - name: name
      value: test
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
