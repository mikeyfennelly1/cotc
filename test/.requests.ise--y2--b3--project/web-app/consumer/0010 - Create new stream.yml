info:
  name: 0010 - Create new stream
  type: http
  seq: 2

http:
  method: POST
  url: "{{nginx}}/api/consumer/streams"
  body:
    type: json
    data: |-
      {
        "name": "test",
        "parent": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
