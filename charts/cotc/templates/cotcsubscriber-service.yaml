apiVersion: v1
kind: Service
metadata:
  name: {{ include "cotc.subscriberServiceName" . }}
  labels:
    {{- include "cotc.labels" . | nindent 4 }}
    {{- include "cotc.selectorLabels" (dict "root" . "component" "cotcsubscriber") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "cotc.selectorLabels" (dict "root" . "component" "cotcsubscriber") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.cotcsubscriber.port }}
      targetPort: http
